# ai-platform-demo - 企业级 AI 应用平台 Demo

用于向管理层汇报的 Demo 全栈应用，展示公司内部 AI 平台架构。

## 项目约定

- 前端：React + Ant Design Pro
- 后端：FastAPI (Python 3.12+)
- 数据库：SQLite
- 部署：Docker Compose
- 配置走 config.yaml + .env

## 架构

五层架构：用户接入层 → Agent编排层 → 能力支撑层 → 模型网关层 → 基础设施层

## 启动方式

```bash
npm install
npm run dev
```
浏览器打开 http://localhost:3000

## 恢复工作

打开后说"继续 AI 平台 Demo"即可恢复上下文。

## 当前状态（2026-04-30）

Demo v2.1 已完成：
- 9个页面：仪表盘、Agent市场、4个Agent演示、模型网关监控、数据安全流转、Agent搭建器、能力管理
- 数据安全流转页重构：路径A/B/C切换 + 4层卡片（出站蓝色→入站紫色）+ 粒子流动画 + 底部场景实例
- 架构升级到 v4.2（Dify+MCP解耦、两阶段安全拦截、NER Sidecar、AI可观测性六维度）
- 仪表盘：能力支撑层优化为3×2紧凑网格、部门名称更新（6个部门）、总裁办排名第2、卡片等高
- Agent演示页改为自动播放（3.5秒/步）+ 历史对话侧栏 + 重置
- 网关监控页新增拦截实时监控（拦截次数/模型延迟/健康状态）
- 能力管理页：知识库文档可点击查看内容、工具卡片可查看接口详情、数据源可查看表结构
- 全站移动端响应式适配
- 全部模拟数据，离线可运行，GitHub Pages 部署
